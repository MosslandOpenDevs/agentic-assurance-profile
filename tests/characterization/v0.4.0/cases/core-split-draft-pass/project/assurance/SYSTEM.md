# System

This deterministic fixture represents a small example service. Its boundary is
the fixture repository, and its known limitation is that it carries no runtime
evidence because it exists only to characterize the mechanical DRAFT gate.
