# Direct dependencies of scripts/validate.py. Regenerate the lock after edits:
#   uv pip compile --universal --generate-hashes --python-version 3.10 \
#     -o requirements-ci.txt requirements.in
pyyaml
jsonschema
